export class Product {
      constructor(public ProductName:string, public StockSize:number, public ProductDescription:string, public SellerName:string, public ProductId:number,public CategoryId: number ) {

      }
}